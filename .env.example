# -----------------------------------------------------------------------------
# Backend (Strapi) environment variables
# -----------------------------------------------------------------------------

# PostgreSQL connection string. Get this from your local DB setup or Railway.
DATABASE_URL=postgresql://user:password@localhost:5432/ostrea_db

# Strapi security keys (comma-separated). Generate strong random values.
APP_KEYS=
API_TOKEN_SALT=
ADMIN_JWT_SECRET=
JWT_SECRET=

# Cloudinary credentials. Obtain these from your Cloudinary dashboard.
CLOUDINARY_NAME=
CLOUDINARY_KEY=
CLOUDINARY_SECRET=

# SMTP2GO API key. Create this in your SMTP2GO account.
SMTP2GO_API_KEY=

# Sender email for transactional notifications.
SMTP2GO_SENDER_EMAIL=noreply@ostrea.uk

# Recipient email for admin form notifications.
ADMIN_NOTIFICATION_EMAIL=info@ostrea.uk

# Optional automation webhook endpoint (Zapier/Make/n8n catch hook URL).
WEBHOOK_URL=

# Shared secret used to sign/verify webhook requests.
WEBHOOK_SECRET=

# Public/backend Strapi base URL used by services and integrations.
STRAPI_URL=http://localhost:1337

# -----------------------------------------------------------------------------
# Frontend (Astro) environment variables
# -----------------------------------------------------------------------------

# Internal Strapi URL used by Astro server-side data fetching.
STRAPI_URL=http://localhost:1337

# API token for authenticated Strapi requests from the frontend server.
STRAPI_API_TOKEN=

# Public Strapi URL exposed to client-side code (must start with PUBLIC_).
PUBLIC_STRAPI_URL=http://localhost:1337
